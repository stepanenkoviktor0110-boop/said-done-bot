---
task: 5
wave: 2
depends: [1, 3, 4]
status: pending
---

# Task 5: Callback handler — 📋 Извлечь задачи

## Description
Handle `action:tasks` callback — run task extraction and show result with rating buttons.

## What to do

In `entrypoints/telegram_bot.py`:
1. New callback handler `@router.callback_query(F.data == "action:tasks")`
2. Validate `_user_state[user_id]["awaiting_action"]` → ACTION_EXPIRED if false
3. Clear buttons (`edit_reply_markup(None)`)
4. Call `extract_tasks([transcript], llm_provider)`
5. Handle markers: no_tasks → NO_TASKS_FOUND, too_many_tasks → TOO_MANY_TASKS, summary → show summary
6. If tasks: send numbered list + rating keyboard [1][2][3][4][5]
7. Update voice_request: action_type="tasks", task_count=N
8. `decrement_trial(user_id)`
9. Set state: `awaiting_feedback=True`, `voice_request_id=vr_id`
10. Log: `action=tasks: userId=X, taskCount=N`

## Verification
Manual: voice → click 📋 → task list + rating buttons. DB: action_type="tasks", trial decreased.

## Files to modify
- `entrypoints/telegram_bot.py`

## Files to read
- `core/task_extractor.py`
- `core/messages.py`
- `core/db_ops.py`
