---
task: 6
wave: 2
depends: [1, 2, 4]
status: pending
---

# Task 6: Multi-voice debounce buffer

## Description
Implement per-chat 3-second debounce buffer for collecting multiple forwarded voices.

## What to do

In `entrypoints/telegram_bot.py`:
1. `_debounce_buffers: dict[int, dict]` — chat_id → {voices: [], timer, reply_ctx}
2. Modify `handle_voice()`: instead of immediate processing, add to buffer + start/reset 3s timer
3. On timer fire: collect all voices, transcribe each, join transcripts
4. Handle partial failures: transcribe what succeeded, note "X из Y не удалось"
5. Enforce MAX_BATCH_SIZE=10
6. Long voice warning if any > 180s
7. Show combined transcript + action buttons

## Verification
Manual: forward 3 voices simultaneously → one combined result. Check logs show 3 transcriptions.

## Files to modify
- `entrypoints/telegram_bot.py`

## Files to read
- `core/transcriber.py`
