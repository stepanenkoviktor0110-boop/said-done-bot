---
task: 8
wave: 3
depends: [1, 4]
status: pending
---

# Task 8: Trial system — counter, exhaustion check, survey trigger

## Description
Implement trial counter: check before processing, trigger survey at 0, block at phase 3.

## What to do

In `entrypoints/telegram_bot.py`:
1. In voice handler (before transcription): check `user["trial_remaining"]`
2. If 0:
   - If trial_phase=1, not blocked, survey_progress<4 → trigger survey (Task 9)
   - If trial_phase=3 → TRIAL_EXHAUSTED, return
3. On successful action completion (both tasks and summary): `decrement_trial(user_id)`
4. Update `/start` response: include trial_remaining count

In `core/db_ops.py` (if not done in Task 1):
- `decrement_trial(user_id)` — MAX(trial_remaining-1, 0)
- `complete_survey(user_id)` — phase=2, trial_remaining=10
- `block_survey(user_id)` — survey_blocked=1

## Verification
Manual: send 20 voices → survey prompt. DB: trial_remaining=0, trial_phase=1, in_survey=True.

## Files to modify
- `entrypoints/telegram_bot.py`
- `core/db_ops.py` (if needed)

## Files to read
- `work/full-mvp/user-spec.md` (Scenario 4)
- `core/messages.py` (TRIAL_EXHAUSTED, SURVEY_INTRO)
