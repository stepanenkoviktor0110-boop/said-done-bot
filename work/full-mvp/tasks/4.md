---
task: 4
wave: 1
depends: []
status: pending
---

# Task 4: Update voice handler — two action buttons instead of one

## Description
Modify existing `handle_voice()` to show [📋 Извлечь задачи] [📝 Сделать резюме] buttons and store state for callback.

## What to do

In `entrypoints/telegram_bot.py`:
1. Add `_user_state: dict[int, dict]` for per-user session state
2. Modify `handle_voice()`:
   - After transcription succeeds, show: "🗣 Распознано: {text}\n\n{ACTION_KEYBOARD}"
   - Inline keyboard with 2 buttons: action:tasks, action:summary
   - Store state: `_user_state[user_id]["awaiting_action"] = True`, `_user_state[user_id]["transcript"] = text`
   - Create voice_request in DB
   - Do NOT auto-extract tasks or summarize

## Verification
Manual: send voice → see transcript + 2 buttons → nothing else happens until button click.

## Files to modify
- `entrypoints/telegram_bot.py`

## Files to read
- `core/messages.py` (action button strings)
- `core/db_ops.py` (create_voice_request)
